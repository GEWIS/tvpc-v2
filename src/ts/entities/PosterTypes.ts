export enum PosterTypes {
  agenda = 'agenda',
  infima = 'infima',
  image = 'image',
  logo = 'logo',
  external = 'external',
  poster = 'poster',
  photo = 'photo',
  train = 'train',
  video = 'video',
}
